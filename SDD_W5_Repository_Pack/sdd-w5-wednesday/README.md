# Wednesday — SOLID + Patterns

## Exercise 4
Inspect `solid_violations.py`, identify one violation for each SOLID principle, record the line/principle/reason/fix in `SPEC.md`, then implement a corrected version.

## Exercise 5
Use `design_pattern_starter.py` to apply one of Strategy, Repository, or Factory to the RetailCo capstone. Write the DDR before implementation and add a unit test proving the pattern's behaviour.
